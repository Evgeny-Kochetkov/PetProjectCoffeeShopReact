const font = 'Merienda, cursive';
const shadow = '0px 4px 4px rgba(0, 0, 0, 0.25)';
const white = '#FFFFFF';
const black = '#000000';

export const theme = {
    font,
    shadow,
    colors: {
        white,
        black
    }
};